A flutter application that returns images from pexels API.

To test:
* Simply clone the application
* Run "flutter pub get" in your terminal
* Then run the application on a simulator/emulator or a real device.
